language: bash

sudo: false

addons:
  apt:
    sources:
      - debian-sid
    packages:
      - shellcheck

script:
 - bash -c 'shopt -s globstar; shellcheck SOURCES/kaosv'
 - bash -c 'shopt -s globstar; shellcheck SOURCES/supervisor'

matrix:
  fast_finish: true
